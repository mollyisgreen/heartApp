# SQLite version 3.x
#   gem install sqlite3
#
#
development:
  adapter: postgresql
  database: heart_app_db
  username: mollyyang
  host: localhost

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  adapter: postgresql
  database: heart_app_db_test
  username: mollyyang
  host: localhost

production:
  adapter: postgresql
  database: heart_app_db_live
  username: mollyyang
  host: localhost
